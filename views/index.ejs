<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background: #f1f1f1;
      display: flex;
      height: 100vh;
      justify-content: center;
      align-items: center;
    }
    .signin-container {
      max-width: 900px;
      margin: 50px auto;
      background: #fff;
      display: flex;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }
    .left-panel {
      flex: 1;
      background: linear-gradient(135deg, #0072ff, #00c6ff);
      color: #fff;
      padding: 50px 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }
    .left-panel h2 {
      font-weight: 700;
    }
    .right-panel {
      flex: 1;
      padding: 40px 30px;
    }
    .form-control {
      margin-bottom: 15px;
      height: 50px;
      border-radius: 8px;
    }
    .btn-main {
      width: 100%;
      height: 50px;
      border-radius: 8px;
      background: #0072ff;
      color: #fff;
      border: none;
    }
    .btn-main:hover {
      background: #005fd4;
    }
    .social-icons i {
      font-size: 24px;
      margin: 0 10px;
      cursor: pointer;
      color: #0072ff;
    }
  </style>
</head>
<body>
  <div class="signin-container">
    
    
    <div class="left-panel">
      <h2>Welcome Back!</h2>
      <p>If you already have an account, please login to continue and access your exclusive benefits.</p>
      <a href="/signup" class="btn btn-light mt-3 px-4 py-2 rounded-pill">Don't have an account? Signup</a>
    </div>


    <div class="right-panel">
      <h2 class="fw-bold mb-4">Login</h2>
      <form method="post" action="">
       
        <div class="mb-3">
          <input
            type="email"
            name="email"
            class="form-control"
            placeholder="Email address"
            required
          />
        </div>
    
        <div class="mb-3">
          <input
            type="password"
            name="password"
            class="form-control"
            placeholder="Password"
            required
          />
        </div>
       
        <button type="submit" class="btn-main">Login</button>
      
        <a href="/signup" class="btn btn-outline-primary mt-3 w-100">Signup</a>
      </form>

      <p class="text-center mt-3">or login with</p>
      <div class="text-center social-icons">
        <i class="bi bi-facebook"></i>
        <i class="bi bi-google"></i>
        <i class="bi bi-linkedin"></i>
      </div>
    </div>
  </div>
</body>
</html>
